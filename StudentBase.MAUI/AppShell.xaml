<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="StudentBase.MAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:StudentBase.MAUI"
    Title="СУБД личных данных студентов"
    FlyoutBackgroundColor="{StaticResource Gray100}" 
    FlyoutBehavior="Locked" 
    FlyoutWidth="200"
    TitleColor="{StaticResource Primary}">

    
    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid Padding="15,15" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Text="●" TextColor="{StaticResource Action}" Margin="0,0,10,0"/>
                <Label Grid.Column="1" 
                        Text="{Binding Title}" 
                        FontAttributes="Bold"
                       FontSize="16"
                        TextColor="{StaticResource Primary}"
                        VerticalOptions="Center"/>
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <FlyoutItem Title="Главная">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Студенты">
        <ShellContent
            ContentTemplate="{DataTemplate local:StudentPage}"
            Route="StudentPage"/>
    </FlyoutItem>

    <FlyoutItem Title="Группы">
        <ShellContent 
            ContentTemplate="{DataTemplate local:GroupPage}"
            Route="GroupPage" />
    </FlyoutItem>

    <FlyoutItem Title="Программы обучения">
        <ShellContent 
            ContentTemplate="{DataTemplate local:ProgramPage}"
            Route="ProgramPage" />
    </FlyoutItem>

    <FlyoutItem Title="Переводы студентов">
        <ShellContent 
            ContentTemplate="{DataTemplate local:StudentTransfersPage}"
            Route="StudentTransfersPage" />
    </FlyoutItem>

    <FlyoutItem Title="Оплата">
        <ShellContent 
            ContentTemplate="{DataTemplate local:PaymentPage}"
            Route="PaymentPage" />
    </FlyoutItem>

</Shell>
